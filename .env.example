# 对外端口
AGENTOS_WEB_PORT=5173
AGENTOS_SERVER_PORT=8080
AGENTOS_POSTGRES_PORT=5433

# PostgreSQL；正式环境请修改
AGENTOS_DATABASE_PASSWORD=agentos

# OpenAI-compatible 模型服务
AGENTOS_MODEL_API_KEY=
AGENTOS_MODEL_ENDPOINT=https://api.minimaxi.com/v1/chat/completions
AGENTOS_MODEL_MODEL=MiniMax-M3
AGENTOS_MODEL_CHAT_MODEL=

# 生产环境请执行 `openssl rand -base64 32` 生成，不要提交真实值
AGENTOS_MODEL_SECRET_KEY=qwKfaFJeiYG6CI6XSq7+jYoM1EH2wg/WHPPl5h84kv8=

# 可选：脚本调用 /api/** 时使用 X-API-Key
AGENTOS_API_KEY=

# 可选：浏览器 JWT 登录。设置管理员密码后会自动启用。
AGENTOS_AUTH_ENABLED=
AGENTOS_AUTH_ADMIN_USER=admin
AGENTOS_AUTH_ADMIN_PASSWORD=
AGENTOS_AUTH_SECRET=
